# Step 6: import-revision-proof-and-review-hardening

Correct and complete step 5 before closing child issue #14. Read step 5, ADR-032, the exact supplier mapping migration, the current sourcing-arrivals UI/data loaders, and the outer audit findings below. Tests first. Keep this correction limited to import/revision review and promotion; do not implement warehouse allocation splitting or receipt variance.

## Required corrections

- Add an executable disposable-DB behavioral fixture/assertion for the step-5 RPCs. Prove user-wide exact hash blocking, two-user hash isolation, edge-only shipment normalization, same logical shipment revision/supersedes linkage, repeated SKU rows remaining separate and ordered, post-receipt supersession rejection, immutable prior rows, mapping blockers, owner checks, atomic promotion, one row/item/default allocation, one promotion only, and no inventory/transaction mutation from registration or promotion. SQL-text tests alone are insufficient.
- Never trust `productVariantId` supplied by a client preview. At registration and again at promotion, resolve/verify every row against the current active exact `(user, supplier, normalized external SKU)` mapping. A missing, inactive, conflicting, cross-owner, or mismatched mapping must remain blocked. Do not allow an arbitrary owned ProductVariant to bypass mapping confirmation.
- Preserve immutable source evidence. Resolve missing mappings before creating the immutable accepted revision, or model an explicit review decision separately without updating raw source values. Do not call the step-3 source-row update path against the canonical immutable-row trigger. Ensure the actual UI offers selection of an existing ProductVariant plus explicit mapping confirmation before final revision acceptance, and can leave/return after product creation.
- Make concurrent same-logical-shipment registration deterministic. Two different hashes racing on the same `(user,supplier,shipment)` must serialize/retry into ordered revisions or return an explicit logical-import conflict suitable for retry; it must not be mislabeled `duplicate_file_hash`. Only an actual `(user,file_hash)` collision may return that error. Concurrent promotion must still create at most one arrival.
- Validate file hashes as lowercase 64-character SHA-256 at the trusted RPC boundary. Validate template/template-version ownership and their mutual relationship rather than relying only on separate FKs.
- Reconcile legacy duplicate non-null hashes before creating the user/hash unique index. Preserve every legacy row, keep one canonical hash claim, and record the ambiguous duplicates as migration exceptions instead of failing migration or deleting evidence.
- Revoke authenticated direct inserts as well as updates/deletes on canonical imports, revisions, source rows, arrivals, items, and allocations where trusted RPCs own creation. RLS plus immutable update/delete triggers do not prevent fabricated evidence through a direct insert.
- Carry the exact raw quantity cell from parser output through preview and persistence. Do not reconstruct it from parsed numeric quantity; values such as `001`, `1,000`, `1.5`, and invalid text must remain unchanged in evidence.
- Make `supabase/schema.sql` genuinely executable and equivalent. The current step-5 declarations are inside a block comment before the canonical tables and therefore install nothing in a schema bootstrap. Place executable declarations after dependent tables/functions and remove misleading commented pseudo-schema.
- Wire a minimal Simple Surface First review/promote path into the current sourcing arrivals surface: ordered original rows, mapping/blocker state, default warehouse selection as the second stage, and an explicit promotion action. Reuse existing tables/toolbars/modals; do not create a parallel workspace. After promotion, the resulting canonical arrival must be visible through the existing arrival loader.
- The file upload/storage sequence must keep clear exact-duplicate errors and compensating cleanup. Do not let cleanup delete another accepted revision's object. Keep storage and external calls inside server actions.

## Acceptance

- Add focused action/component/domain tests for mapping confirmation, rejection of forged/stale ProductVariant ids, leave-and-return review, default warehouse promotion, explicit duplicate/revision outcomes, and storage cleanup ownership.
- Extend `scripts/fixtures/inbound-canonical-assertions.sql` (and its legacy fixture only where necessary) so the existing `npm run test:inbound-behavioral` proves all new database invariants against the complete migration chain.
- Run the dedicated Docker behavioral proof in the outer environment when available, focused and full tests, Prisma validation, lint, production build, and `git diff --check`.
